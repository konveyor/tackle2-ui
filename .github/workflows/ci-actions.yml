name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  unit-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
      - name: Build
        run:
          npm install
          npm install -w pkg/client
          npm run build -w pkg/client
      - name: Test
        run: 
          npm run test -w pkg/client
      - uses: codecov/codecov-action@v1
        with:
          flags: unitests
