import * as unknownTagsActions from "./actions";
import {
  reducer as unknownTagsReducer,
  stateKey as unknownTagsStateKey,
} from "./reducer";
import * as unknownTagsSelectors from "./selectors";

export {
  unknownTagsActions,
  unknownTagsReducer,
  unknownTagsSelectors,
  unknownTagsStateKey,
};
